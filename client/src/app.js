import React from 'react';
import ReactDOM from 'react-dom';
import 'normalize.css/normalize.css';
import './styles/styles.scss'

// Change name of the app in package.json and index.html!!!
// Change 'author' in package.json
// Change the icon (./public/img)!!!
// Install all the modules so everything is set up!!!

ReactDOM.render(<p>Boilerplate</p>, document.querySelector('#app'));
